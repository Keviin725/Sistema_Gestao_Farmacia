import 'package:flutter/material.dart';

class ListaRelatoriosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lista de Relatórios'),
      ),
      body: Center(
        child: Text('Lista de Relatorios em construção...'),
      ),
    );
  }
}
