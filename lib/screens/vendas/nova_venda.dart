import 'package:flutter/material.dart';

class NovaVendaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nova Venda'),
      ),
      body: Center(
        child: Text('Tela de Nova Venda em construção...'),
      ),
    );
  }
}
