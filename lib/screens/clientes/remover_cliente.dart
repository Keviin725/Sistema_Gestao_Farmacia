import 'package:flutter/material.dart';

class RemoverClienteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Remover Cliente'),
      ),
      body: Center(
        child: Text('Tela de Remover Cliente em construção...'),
      ),
    );
  }
}
