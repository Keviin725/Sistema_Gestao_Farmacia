import 'package:flutter/material.dart';

class ListaStockScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lista de Stock'),
      ),
      body: Center(
        child: Text('Lista de Stock em construção...'),
      ),
    );
  }
}
