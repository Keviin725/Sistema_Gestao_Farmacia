import 'package:flutter/material.dart';

class AtualizarInventarioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Atualizar Stock'),
      ),
      body: Center(
        child: Text('Tela de Atualizar Stock em construção...'),
      ),
    );
  }
}
